{ "datasource" => {
    "{{ name }}" => {
            "pool-name" => "{{ name }}",
		    "use-java-context" => "true",
			"connection-url" => "jdbc:postgresql://localhost:5432/mjolnir",
            "driver-name" => "postgresql",
            "enabled" => true,
            "jndi-name" => "java:jboss/datasources/mjolnir/MjolnirDS",
            "password" => "loki",
            "user-name" => "ohchie8J"
      }
   }
}
