{ "jdbc-driver" =>
  { "<%= driver_name %>" => {
        "driver-name" => "<%= driver_name %>",
        "driver-module-name" => "<%= module_name %>",
        "driver-xa-datasource-class-name" => "<%= xa_datasource_class_name %>"
    }
  }
}

